spec:
  template:
    spec:
      containers:
        - name: scoring-service
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
              scheme: HTTP
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 3
            successThreshold: 1
            timeoutSeconds: 1